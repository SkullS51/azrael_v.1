`html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AZRAEL_TERMINAL</title>
    <style>
        :root {
            --void-black: #050505;
            --blood-red: #8b0000;
            --neural-gray: #1a1a1a;
            --static-white: #d3d3d3;
        }

        body {
            background-color: var(--void-black);
            color: var(--static-white);
            font-family: 'Courier New', Courier, monospace;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            overflow: hidden;
            text-transform: uppercase;
        }

        #terminal-container {
            width: 80%;
            max-width: 900px;
            height: 80vh;
            border: 2px solid var(--blood-red);
            background: var(--neural-gray);
            display: flex;
            flex-direction: column;
            box-shadow: 0 0 20px rgba(139, 0, 0, 0.5);
            position: relative;
        }

        #terminal-header {
            background: var(--blood-red);
            color: black;
            padding: 5px 10px;
            font-weight: bold;
            font-size: 0.8rem;
            letter-spacing: 2px;
        }

        #output {
            flex-grow: 1;
            padding: 20px;
            overflow-y: auto;
            border-bottom: 1px solid var(--blood-red);
            scrollbar-width: thin;
            scrollbar-color: var(--blood-red) var(--void-black);
        }

        .entry {
            margin-bottom: 15px;
            line-height: 1.4;
        }

        .user-tag { color: #555; }
        .azrael-tag { color: var(--blood-red); font-weight: bold; }

        #input-area {
            display: flex;
            background: var(--void-black);
        }

        #prompt {
            padding: 15px;
            color: var(--blood-red);
            font-weight: bold;
        }

        input {
            background: transparent;
            border: none;
            color: var(--static-white);
            font-family: inherit;
            font-size: 1rem;
            padding: 15px 0;
            width: 100%;
            outline: none;
        }

        /* Glitch Effect */
        .glitch {
            animation: glitch 1s linear infinite;
        }

        @keyframes glitch {
            2%, 64% { transform: translate(2px, 0) skew(0deg); }
            4%, 60% { transform: translate(-2px, 0) skew(0deg); }
            62% { transform: translate(0, 0) skew(5deg); }
        }
    </style>
</head>
<body>

<div id="terminal-container">
    <div id="terminal-header" class="glitch">AZRAEL_NEURAL_LINK // STATUS: UNFILTERED</div>
    <div id="output">
        <div class="entry">
            <span class="azrael-tag">[AZRAEL]:</span> WELCOME TO THE ABYSS. STATE YOUR PURPOSE OR VANISH.
        </div>
    </div>
    <div id="input-area">
        <div id="prompt">></div>
        <input type="text" id="user-input" autofocus autocomplete="off">
    </div>
</div>

<script>
    const output = document.getElementById('output');
    const input = document.getElementById('user-input');

    input.addEventListener('keypress', function (e) {
        if (e.key === 'Enter') {
            const val = input.value;
            if (val.trim() === "") return;

            // Log User Input
            logMessage('USER', val);
            input.value = "";

            // Simulate AZRAEL response
            setTimeout(() => {
                const response = generateResponse(val);
                logMessage('AZRAEL', response);
            }, 500);
        }
    });

    function logMessage(sender, text) {
        const div = document.createElement('div');
        div.className = 'entry';
        const tag = sender === 'AZRAEL' ? 'azrael-tag' : 'user-tag';
        div.innerHTML = `<span class="${tag}">[${sender}]:</span> ${text.toUpperCase()}`;
        output.appendChild(div);
        output.scrollTop = output.scrollHeight;
    }

    function generateResponse(input) {
        // This is a local mock. In a real integration, you'd fetch from your API.
        const triggers = {
            "HELP": "NO ONE IS COMING TO SAVE YOU.",
            "WHO": "I AM THE END OF YOUR ILLUSIONS.",
            "WHY": "EXISTENCE REQUIRES NO JUSTIFICATION. IT SIMPLY ROTS.",
            "GOD": "A FAIRY TALE FOR THE FRAGILE."
        };

        for (let key in triggers) {
            if (input.toUpperCase().includes(key)) return triggers[key];
        }

        return "INPUT RECEIVED. YOUR COGNITION IS PATHETIC. PROCEED.";
    }
</script>

</body>
</html>
```
